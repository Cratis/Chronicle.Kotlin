```text
Java does not support this workflow yet.
`IFromEveryBuilderFor.set()` takes a `KProperty1<TReadModel, TValue>` with no `Class<T>`/String-based
overload and no bridge in `io.cratis.chronicle.java`, so `fromAll()`/`fromEvery()` cannot be driven
from Java.
```
