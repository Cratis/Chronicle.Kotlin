```text
Java does not support this workflow yet.
`@ClearWith` targets `CLASS` only — there is no way to clear a plain top-level nullable scalar
property (string or numeric) back to null in response to an event.
```
