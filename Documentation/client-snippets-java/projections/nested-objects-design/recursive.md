```text
Java does not support this workflow yet.
Without a `.nested()`/`.clearWith()` equivalent, there is no way to express
even a single level of nested sub-object projection, let alone the recursive
(nested-within-nested) case. Track the client SDK issue before relying on
nested object projection from Java.
```
