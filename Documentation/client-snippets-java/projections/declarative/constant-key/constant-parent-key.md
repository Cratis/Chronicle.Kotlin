```text
Java does not support this workflow yet.
`IChildFromBuilderFor` only has `usingKey(String)` and `usingParentKey(String)` — there is no
`usingConstantParentKey` for targeting the same parent instance from every child event.
```
