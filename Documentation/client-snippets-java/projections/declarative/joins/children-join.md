```text
Java does not support this workflow yet.
`IChildrenBuilderFor` has no `join()` method — joins are only available on the top-level
`IProjectionBuilderFor<TReadModel>`, not inside a child collection scope.
```
