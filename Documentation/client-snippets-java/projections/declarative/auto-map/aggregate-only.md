```text
Java does not support this workflow yet.
The declarative fluent builder has no aggregate operators (Count/Increment/Decrement/Add/Subtract) —
those exist only as property annotations (`@Count`, `@Increment`, `@Decrement`, `@AddFrom`,
`@SubtractFrom`) on model-bound read models, not on `IFromBuilderFor`/`ISetBuilderFor`.
```
