```text
Java does not support this workflow yet.
`IProjectionBuilderFor` has no AutoMap toggle at all — no `noAutoMap()`/`autoMap()` method on the
builder or on `IChildrenBuilderFor`, so there is no child scope to re-enable AutoMap on.
```
