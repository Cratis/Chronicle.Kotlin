```text
Java does not support this workflow yet.
`IChildrenBuilderFor`/`IChildFromBuilderFor` have no `nested()` method — `nested()` exists only on
the top-level `IProjectionBuilderFor<TReadModel>`, so a nested object cannot be declared on a child
item inside a `children()` scope.
```
