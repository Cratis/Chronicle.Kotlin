```text
Java does not support this workflow yet.
The fluent `IProjectionBuilderFor` API has no counting equivalent to C#'s
`.Count()` — track the client SDK issue before relying on declarative
event counting from Java.
```
