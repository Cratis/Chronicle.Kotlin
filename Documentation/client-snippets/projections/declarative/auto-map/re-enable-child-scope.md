```text
Kotlin does not support this workflow yet.
`IProjectionBuilderFor` has `noAutoMap()` and `autoMap()`, but `IChildrenBuilderFor` has no
AutoMap toggle, so there is no child scope to re-enable AutoMap on.
```
