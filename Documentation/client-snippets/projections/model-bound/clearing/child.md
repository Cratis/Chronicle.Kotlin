```text
Kotlin does not support this workflow yet.
`@ClearWith` targets `CLASS` only, so it cannot sit on a property inside a `@ChildrenFrom` element
type — there is no way to clear a single member of a child item back to null in response to an event.
```
