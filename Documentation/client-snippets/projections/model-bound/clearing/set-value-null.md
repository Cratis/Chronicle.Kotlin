```text
Kotlin does not support this workflow yet.
There is no `@SetValue` annotation in this client — `@SetFrom` only maps a property from an event
property path, it cannot set a literal constant (including `null`).
```
